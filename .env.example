# === ACCESS TO YOUR API ===
ACCESS_KEY=replace-with-long-random-secret

# === SITE CREDS (Bitrix) ===
BASE_URL=https://www.replace.me
LOGIN_PATH=/auth.php             # adjust if different (e.g., /?login=yes or /auth/)
LOGIN_USER=123456789
LOGIN_PASSWORD=REPLACE_ME
AFTER_LOGIN_BACKURL=/personal/access/

# === FORM CONSTANTS (leave as-is unless you change your data) ===
DEFAULT_RUS_CODE=000
TYPE=1
SPECIAL_TYPE_ANY_CAR=1
SELECT_HOUSE=
SELECT_FLAT=
FIO=
PHONE=
COMMENT=
AJAX_EDIT_TICKET=true


TZ=Europe/Moscow
APP_TZ=Europe/Moscow
USER_AGENT=
BITRIX_TIMEOUT_MS=30000

# === TELEGRAM BOT ===
TELEGRAM_BOT_TOKEN=REPLACE_TELEGRAM_BOT_TOKEN
TELEGRAM_WEBHOOK_SECRET=replace-webhook-secret   # you will use this in webhook URL ?secret=...
BOT_PASSWORD=REPLACE_BOT_PASSWORD               # users must send this once; then they are whitelisted

# === YANDEX SPEECH STT ===
YANDEX_STT_API_KEY=
YANDEX_STT_FOLDER_ID=
YANDEX_STT_LANG=ru-RU
YANDEX_STT_TOPIC=general

# === EDGE CONFIG (trust list storage) ===
EDGE_CONFIG_ID=REPLACE_EDGE_CONFIG_ID
VERCEL_ACCESS_TOKEN=REPLACE_VERCEL_TOKEN
TEAM_ID=
